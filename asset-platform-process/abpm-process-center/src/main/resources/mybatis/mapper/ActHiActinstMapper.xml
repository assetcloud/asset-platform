<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asset.mapper.ActHiActinstMapper">

    <select id="selectUnCompleteTaskByExeId" resultType="com.asset.entity.ActHiActinst">
        SELECT
                *
        FROM
             ACT_HI_ACTINST act
                 INNER JOIN ACT_RU_TASK ru on ru.ID_ = act.TASK_ID_
        WHERE ru.EXECUTION_ID_ = #{executionId};
    </select>

    <select id="selectUnCompleteTaskByInstId" resultType="com.asset.entity.ActHiActinst">
        SELECT
                *
        FROM
             ACT_HI_ACTINST act
                 INNER JOIN ACT_RU_TASK ru on ru.ID_ = act.TASK_ID_
        WHERE ru.PROC_INST_ID_ = #{instId};
    </select>

</mapper>
