<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asset.dao.GroupMapper">

    <insert id="createGroup" parameterType="com.asset.entity.GroupCreateInfo">
        INSERT INTO asset_oapp_group
        (oapp_id,
        name,
        status)
        VALUES
        (
        #{oapp_id,jdbcType = VARCHAR},
        #{group_name,jdbcType = VARCHAR},
        #{status,jdbcType = INTEGER}
        )
    </insert>

    <update id="renameGroup" parameterType="com.asset.entity.GroupRenameInfo">
      UPDATE asset_oapp_group
      SET
      group_name = #{newName,jdbcType = VARCHAR},
      WHERE
      group_id = #{groupID,jdbcType = VARCHAR}
    </update>

    <update id="deleteGroup" parameterType="com.asset.entity.GroupDeleteInfo">
        UPDATE asset_oapp_group
        SET
        status = #{disableParam,jdbcType = INTEGER}
        WHERE
        group_id = #{groupID,jdbcType = VARCHAR}
    </update>

</mapper>
